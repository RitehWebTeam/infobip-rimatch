import * as SettingsCard from "@/components/GenericCard";

const BlockedUsers = () => {
  return (
    <SettingsCard.Root>
      <SettingsCard.Header title="Blocked Users" />
      <div className="flex flex-col w-full px-4 gap-3">
        {/* Placeholder for future content */}
      </div>
    </SettingsCard.Root>
  );
};

export default BlockedUsers;
